{
  "name": "random-number",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
   "watch": "babel -w src/ -d dist/",
   "build": "babel src/ -d dist/",
   "serve": "babel src/ -d dist/ | nodemon --watch dist",
   "test": "echo \"Error: no test specified\" && exit 1",
   "start": "babel-node --presets es2015 app.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {   
    "express": "^4.15.2"
  },
  "devDependencies": {
    "babel": "^6.23.0",
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "nodemon": "^1.11.0"
  }
}
